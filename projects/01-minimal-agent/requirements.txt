# Core agent dependencies（最小依赖原则）
# 没有 LangChain / LangGraph——这是手写 Agent，目标是理解原理

pydantic>=2.0          # 用于结构化模型输出
python-dotenv>=1.0     # 读 .env
# requests>=2.31      # 仅当需要 HTTP 工具时再加

# 开发依赖（可选）
# pytest>=7.0
# rich>=13.0           # 漂亮打印 trace